<p id="notice"><%= notice %></p>

<h1>Cadastro de Stormtroopers</h1>

<table class="table table-bordered table-hover .table-striped">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Patente</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @soldados.each do |soldado| %>
      <tr>
        <td><%= soldado.nome %></td>
        <td><%= soldado.patente %></td>
        <td><%= link_to 'Detalhes', soldado %></td>
        <td><%= link_to 'Editar', edit_soldado_path(soldado) %></td>
        <td><%= link_to 'Jogar no Vácuo', soldado, method: :delete, data: { confirm: 'Tem Certeza' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo Stormtrooper', new_soldado_path, :class => "btn btn-primary" %>
